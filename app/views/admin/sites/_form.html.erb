<% semantic_form_for([:admin, @site]) do |f| %>
  <%= f.error_messages %>
	<% f.inputs :title => 'Add/Edit Site' do %>
		<%= f.input :url %>
		<%= f.input :title %>
		<%= f.input :description %>
		<%= f.buttons %>
	<% end -%>
<% end %>